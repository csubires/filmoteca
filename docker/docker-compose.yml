services:
  flask:
    image: filmoteca-flask:latest
    container_name: filmoteca-flask
    restart: unless-stopped
    volumes:
      - "/home/user/docker-app/webserver/filmoteca/filmoteca:/usr/src/app/filmoteca"
      - "/etc/timezone:/etc/timezone:ro"
      - "/etc/localtime:/etc/localtime:ro"
      - "/mnt/hgfs/films:/mnt/hgfs/movies:ro"
      - "/mnt/hgfs/ext_films:/mnt/hgfs/ext_movies:ro"
    environment:
      - FLASK_ENV=production
    command: sh -c "uwsgi --ini /usr/src/app/filmoteca/docker/wsgi.ini"
    networks:
      - netflask

  flask2:
    image: filmoteca-flask:latest
    container_name: filmoteca-flask2
    restart: unless-stopped
    volumes:
      - /home/user/docker-app/webserver/filmoteca/filmoteca:/usr/src/app/filmoteca
      - "/etc/timezone:/etc/timezone:ro"
      - "/etc/localtime:/etc/localtime:ro"
      - "/mnt/hgfs/films:/mnt/hgfs/movies:ro"
      - "/mnt/hgfs/ext_films:/mnt/hgfs/ext_movies:ro"
    environment:
      - FLASK_ENV=development
    command: sh -c "uwsgi --ini /usr/src/app/filmoteca/docker/wsgi2.ini"
    networks:
      - netflask

  nginx:
    image: nginx:latest
    container_name: nginx-flask
    restart: unless-stopped
    volumes:
      - "./filmoteca/docker/nginx.conf:/etc/nginx/conf.d/nginx.conf"
      - "/var/log/nginx-flask:/var/log/nginx"
      - "/etc/timezone:/etc/timezone:ro"
      - "/etc/localtime:/etc/localtime:ro"
    command: sh -c "rm /etc/nginx/conf.d/default.conf"
    ports:
      - 8080:80
      - 8084:81
    depends_on:
      - filmoteca-flask
      - filmoteca-flask2
    networks:
      - netflask

networks:
  netflask:
